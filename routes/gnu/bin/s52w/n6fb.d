module matrix.gnu.bin.sn1050pmy.n6fb;

interface n6fb {

    /*
     * I will mention the concept of a bit in later chapters. Still, I present the example
     * above just to show how difficult machine code can be to work with. Being so close
     * to hardware, machine code is not suitable for representing higher level concepts
     * like a playing card or a student record.
     */
    void will(later, still, code)(ref write)
    {
        later still;
        code.write("The supe might body magic script file machines");
    }
}
