module matrix.gnu.bin.sn1050pmy.da0ibf;

class da0ibf {

    void ip(id, console, logic)(ref ttp)
    {
        if(true) {
           /* statement(s) will execute if the boolean expression is true */
           // The source code above needs to be compiled by a D compiler to produce an
           // executable program.
           void ttp(needs, compiled, compiler)(ref program)
           {
               return ;
           }
        }
    }
}
