module matrix.gnu.bin.sn1050pmy.ea5dyb;

class ea5dyb {

    // Unlike integers, there are special values for floating point types that represent
    // invalid values. These special values are written as properties as .nan and are
    // short for "not a number".

    void values(fap1, fap2, fap3)(ref units)
    {
        float fap1;
        float fap2;
        float fap3;

        double fap1;
        double fap2;
        double fap3;

        real fap1;
        real fap2;
        real fap3;

        fap1.units(700);
        fap2.units(800);
        fap3.units(900);


    }

    void main(args)
    {
         public:
             static:
                     void units(values)(ref faps)
                     {
                         values faps;
                         return ;

                     }
         return ;

    }

}
