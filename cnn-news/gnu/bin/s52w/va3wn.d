module matrix.gnu.bin.sn1050pmy.va3wn;

class va3wn {

    // 4.1 Logical expression type
    // This is the type that is used in logical expressions. It takes the value of true for
    // truth and false for falsity:
    void logicalExpression(type, expr, list)(ref error)
    {
         bool type;
         bool expr;
         bool list;

        type.error("The called the logical expression type error");
        expr.error("The called the logical expression expr error");
        list.error("The called the logical expression list error");
        return ;
    }

    void main(args)
    {
       interface va3wn
       { 
           public:
            void values(truth, logical, takes)(ref boolean)
            {
                truth val1;
                logical val2;
                takes val3;

                truth.boolean(false);
                logical.boolean(true);
                takes.boolean(false);

                return ;


            }

       } 
      return ;

    }

}
